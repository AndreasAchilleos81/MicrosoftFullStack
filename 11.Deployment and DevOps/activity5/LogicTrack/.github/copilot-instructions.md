# Copilot Instructions

## Project Guidelines
- The user prefers zero-based paging (page = 0 for first page) and requested timing instrumentation be returned in response bodies. Also added caching and response envelope middleware.