sudo docker pull mcr.microsoft.com/mssql/server:2017-latest

docker run -v C:/Users/a.achilleos/code/Databases:/adventureWorks  -e "ACCEPT_EULA=Y" -e "SA_PASSWORD=Password1" -p 1433:1433 --name AdventureWorks -d mcr.microsoft.com/mssql/server:2022-latest


With Volume:
docker run -v C:/Users/a.achilleos/code/Databases:/adventureWorks  -e "ACCEPT_EULA=Y" -e "SA_PASSWORD=Password1" -p 1433:1433 --name AdventureWorks -d mcr.microsoft.com/mssql/server:2022-latest

#####
Mysql:

version: '3.8'

services:
  mysql:
    image: mysql:oraclelinux9
    container_name: mysql-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Password1
      MYSQL_DATABASE: technocorp
      MYSQL_USER: user
      MYSQL_PASSWORD: Password1
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    # networks:
    #   - backend

volumes:
  mysql_data:
    driver: local

# networks:
#   backend:
#     driver: bridge
